[package]
name = "remux-dashboard"
version = "0.0.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.7", features = ["web", "router"] }
serde = { version = "1", features = ["derive"] }
#reqwest = { version = "0.12", default-features = false, features = ["json"] }
gloo-timers = { version = "0.3", features = ["futures"] }
gloo-storage = { version = "0.3" }
remux-sdks = { path = "../remux-sdks" }
futures = { version = "0.3", default-features = false, features = ["alloc", "async-await"] }
uuid = { version = "1", features = ["v4", "serde", "js"] }
#http = "1"
web-sys = { version = "0.3", features = ["Window", "Location", "Document", "Element", "Navigator", "Clipboard", "Event", "EventTarget"] }
wasm-bindgen = "0.2"
js-sys = "0.3"
serde_json = "1"
base64 = "0.22"
urlencoding = "2.1.3"

[profile.release]
opt-level = "z"
lto = true
